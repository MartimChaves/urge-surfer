// Development defaults. The production container replaces this file while
// building, so release-only choices are visible and auditable as plain data.
export const JUST_WRITE = true;
export const PEN_LAG_CONTROL = true;
